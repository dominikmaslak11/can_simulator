version: '3.8'

services:
  can-simulator:
    build: .
    container_name: can_simulator
    network_mode: host
    privileged: true
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./data:/app/data
    stdin_open: true
    tty: true
